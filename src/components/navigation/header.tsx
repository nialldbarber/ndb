import React, {FC} from 'react';
import Navigation from 'components/navigation/index';

const Header: FC = () => {
  return (
    <header>
      <Navigation />
    </header>
  );
};

export default Header;
